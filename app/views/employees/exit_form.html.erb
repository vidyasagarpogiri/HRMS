

  <div id="page-wrapper">
                <div class="row">
                <div class="col-lg-12">
                    <h2 class="page-header">Exit Details</h2>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-body">
                            <div class="row">
                                <div class="col-lg-6">
                                    <%= @employee.errors.full_messages.first if @employee.errors.any? %>
                                    <%= form_for @employee, :url =>  update_exit_form_employee_path(@employee), method: :post  do |e| %>
                                        <div class="form-group">
                                            <label class="required" >Date of Exit</label>
                                            <%= date_field(:employee, :date_of_exit, :class=>"form-control") %>
                                                 
                                        </div>
                                        <div class="form-group">
                                            <label class="required" >Status of F & F</label>
                                            <%= e.collection_select :ff_status_id, FfStatus.all, :id, :status_name, { :prompt => "Please Select" }, { :class => "department form-control" } %>
                                                                          
                                        </div>             
                                        <br>
                                        <button type="submit" class="btn btn-success pull-left">Save & Continue<a></button>
                                                    
                                    <% end %>
                                </div>
                                <!-- /.col-lg-6 (nested) -->
                                <div class="col-lg-6">
                                    <h1></h1>
                                    <form role="form">
                                    <!--    <fieldset disabled> -->                                                                           
                                    </form>
                                </div>
                                <!-- /.col-lg-6 (nested) -->
                            </div>
                            <!-- /.row (nested) -->
                        </div>
                        <!-- /.panel-body -->
                    </div>
                    <!-- /.panel -->
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
           
         </div>
