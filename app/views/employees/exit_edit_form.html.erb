

  <div id="page-wrapper">
                <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Exit Details</h1>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-body">
                            <div class="row">
                                <div class="col-lg-6">

                                    <%= form_for @employee, :url =>  update_exit_form_employee_path(@employee), method: :post  do |e| %>
                                        <div class="form-group">
                                            <label>Date of Exit</label>
                                            <%= date_field(:employee, :date_of_exit, :class=>"form-control") %>
                                                 
                                        </div>
                                        <div class="form-group">
                                            <label>Status of F & F</label>
                                            <%= e.collection_select :ff_status_id, FfStatus.all, :id, :status_name, { :prompt => "Please Select" }, { :class => "department form-control" } %>
                                                                          
                                        </div>             
                                        <br>
                                        <button type="submit" class="btn btn-success pull-left">Save & Continue</button>
																				<%= link_to 'Cancel', show_exit_employee_path(@id), :class => "btn btn-success pull-left" %>
                                                    
                                    <% end %>
                                </div>
                                <!-- /.col-lg-6 (nested) -->
                                <div class="col-lg-6">
                                    <h1></h1>
                                    <form role="form">
                                    <!--    <fieldset disabled> -->                                                                           
                                    </form>
                                </div>
                                <!-- /.col-lg-6 (nested) -->
                            </div>
                            <!-- /.row (nested) -->
                        </div>
                        <!-- /.panel-body -->
                    </div>
                    <!-- /.panel -->
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
           
         </div>
