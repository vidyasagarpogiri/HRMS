
   <div class="module-head">
      <p style="color:red;"><u>Note:</u></p><p>Basic, Esic, Esic contribution depends on Gross</p>
      <p>PF,Pf Contribution depends on basic</p>
   </div>
   <%= form_for @static_salary do |s|%>                   
    <div class="module-body">
         <table class="table table-striped table-hover" id="sample-table-2">
							<thead>
								<tr>
									<th>Salary Type</th>
									<th>Percentage</th>
								 <tr/>
							 </thead>	
							 </tbody>				
									<% @static_salaries.each do |salary, i| %>
										<%= fields_for "salary[]", salary do |f| %>   
											<tr>
                          <td><%= salary.name %></td>
                          <td><%= f.text_field :value,:placeholder =>"Enter Value", :data => {:error => "Please Enter Value"}, :required => true,:type => "number"%></td>
                      <tr>
		          <% end %>
		          <% end %>
		          </tbody>
		        
		          <%= submit_tag "Submit", :class => "btn btn-success pull-right", :id=> "save" %><br/>
		          </table>
		        </div>
		          <% end %>


