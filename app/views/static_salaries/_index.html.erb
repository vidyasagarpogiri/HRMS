   <div class="module-head">
    <p style="color:red;"><u>Note:</u></p>Basic, Esic, Esic contribution depends on Gross&nbsp&nbsp&nbsp&nbsp
      AND &nbsp&nbsp&nbsp&nbspPF,Pf Contribution depends on basic
   </div>                  
    <div class="module-body">
      <table class="table table-striped table-hover" id="sample-table-2">
				<thead>
					<tr>
						<th>Salary Type</th>
						<th>percentage</th>
					</tr>
					</thead>	
					</tbody>
						<% @static_salaries.each do |salary| %>  
					<tr>
             <td><%= salary.name %></td>
             <td><%= salary.value %></td>
          </tr>
		        <% end %>
          </tbody>
		    </table>
      </div>
