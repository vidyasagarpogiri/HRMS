   <div class="module-head">
    <p style="color:red;"><u>Note:</u></p><p>Basic, Esic, Esic contribution depends on Gross</p>
      <p>PF,Pf Contribution depends on basic</p>
   </div>                  
    <div class="module-body">
      <table class="table table-striped table-hover" id="sample-table-2">
				<thead>
					<tr>
						<th>Salary Type</th>
						<th>Percentage</th>
					</tr>
					</thead>	
					</tbody>
						<% @static_salaries.each do |salary| %>  
					<tr>
             <td><%= salary.name %></td>
             <td><%= salary.value %></td>
          </tr>
		        <% end %>
          </tbody>
		    </table>
      </div>

