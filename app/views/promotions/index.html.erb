
<div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <h2 class="page-header">Promotions Details</h2>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
<% if current_user.employee.role.id == 2 %>
                    <label style="font-weight:normal;padding:20px;" class = "pull-right"><i class="fa fa-plus "></i> <%= link_to ' New Promotion', new_employee_promotion_path(@employee.id)  %></label>

<% end %><% if @promotions.present? %>
                        <div class="panel-body">
                            <div class="row col-lg-12">
                                  <% @promotions.each do |promotion| %>

                                     <div class="col-lg-6" style="padding:0px">
                                        <div class="form-group">
                                            <label style="font-weight:normal;"> Date Of Promotion:</label>
                                            <label><%= promotion.date_of_promotion %></label>                                          
                                        </div>
                                        <div class="form-group ">
                                          <label style="font-weight:normal;">Designated To:</label>
                                          <label><%= promotion.designation.designation_name  if promotion.designation.present? %> </label>       
                                        </div>                                   
                                   

                                       <% if current_user.employee.role.id == 2 %>
                                       <ul class="navigation-btns" style="list-style: none;display:inline;">
                                         <li><%= link_to 'Edit', edit_employee_promotion_path(@employee.id, promotion.id), :class => "btn btn-success pull-left" %></li>
                                         <li><%= link_to "Delete", employee_promotion_path(@employee, promotion), method: :delete, data: {confirm: "You sure?"}, :class=>"btn btn-danger pull-right" %></li>
                                         
                                         
                                       </ul>
                                      </div>
                                      <br>
                                          <% end %>
                                      <% end %>

 <% else %>
<br/>
<h4>No Promotion Details</h4>
<% end %>
                             
                            <!-- /.row (nested) -->
                        </div>
                        <!-- /.panel-body -->
                    </div>
                    <!-- /.panel -->
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
        </div>
        <!-- /#page-wrapper -->
 
  
