
<div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <h2 class="page-header">Promotions Details</h2>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
<% if current_user.employee.role.id == 2 %>
                     <%= link_to 'Add New Promotion', new_employee_promotion_path(@employee.id), :class => "btn btn-success pull-right" %>

<% end %>
                        <div class="panel-body">
                            <div class="row">
<% if @promotions.present? %>
                                  <% @promotions.each do |promotion| %>
                                        <div class="form-group col-lg-12">
                                <div class="col-lg-12">
                                        <div class="form-group">
                                            <label style="font-weight:normal;"> Date Of Promotion:</label>
                                            <label><%= promotion.date_of_promotion %></label>                                          
                                        </div>
                                        <div class="form-group ">
                                          <label style="font-weight:normal;">Designated To:</label>

                                          <label><%= promotion.designation.designation_name  if promotion.designation.present? %>  <label>                                           
                                        </div>                                   

                                        </div>

                                      
                                       <% if current_user.employee.role.id == 2 %>
                                         <%= link_to 'EDIT', edit_employee_promotion_path(@employee.id, promotion.id), :class => "btn btn-success pull-right" %>
                                         <%= link_to "Delete", employee_promotion_path(@employee, promotion), method: :delete, data: {confirm: "You sure?"}, :class=>"btn btn-success pull-right" %>

                                          <% end %>
<% end %>

 <% else %>
<br/>
<h4>No Promotion Details</h4>
<% end %>
                                </div>
                                <!-- /.col-lg-6 (nested) -->

                            </div>
                            <!-- /.row (nested) -->
                        </div>
                        <!-- /.panel-body -->
                    </div>
                    <!-- /.panel -->
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
        </div>
        <!-- /#page-wrapper -->
 
  
