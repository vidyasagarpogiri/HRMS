<div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <h2 class="page-header">Experience Details</h2>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
	                      <% if current_user.employee.role.id == 2 %>
                        <label style="font-weight:normal;padding:20px;" class = "pull-right"><i class="fa fa-plus "></i> <%= link_to 'Add Experience',  new_employee_experience_path(@employee.id) %></label>
                        <% end %>

                        <div class="panel-body">
                            <div class="row">
                                <div class="col-lg-12">
                                <% if  @experinces.present? %>                         
                                   <% @experinces.each do |experience| %>
                                      <label class="normalfont">Company Name: </label><label>&nbsp<%= experience.previous_company %></label><br>
                                      <label class="normalfont">Designation: </label><label>&nbsp<%= experience.last_designation %></label><br>
                                      <label class="normalfont">Worked From: </label><label>&nbsp<%= format_exp_date(experience.from_date) %></label><label class="normalfont">&nbsp To: </label><label>&nbsp<%= format_exp_date(experience.to_date) %>&nbsp ( <%= difference_in_months(experience.from_date,experience.to_date )%>)</label>
                                                                           
                                        <% if current_user.employee.role.id == 2 %>
                                        <ul class="navigation-btns pull-right" style="list-style: none;display:inline;">
                                          <li><%= link_to 'Edit', edit_employee_experience_path(@employee.id, experience.id), :class => "btn btn-success" %></li>                        
                                          <li><%= link_to "Delete", employee_experience_path(@employee, experience.id), method: :delete, data: {confirm: "You sure?"}, :class=>"btn btn-danger " %></li>
																				<%end %>                                          
                                        </ul>
                                        <br>
                                          <hr>
                                  <% end %>
    <% else %>
																								<h4>No Experience Details </h4>      
                                   <% end %>
                               
                                </div>
                                <!-- /.col-lg-6 (nested) -->
                                <div class="col-lg-6">
                                    <h1></h1>
                                    <form role="form">
                                    <!--    <fieldset disabled> -->
                                         
              
                                    </form>
                                </div>
                                <!-- /.col-lg-6 (nested) -->
                            </div>
                            <!-- /.row (nested) -->
                        </div>
                        <!-- /.panel-body -->
                    </div>
                    <!-- /.panel -->
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
        </div>
