<div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
             <!--       <h2 class="page-header">Education Details</h2> -->
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-body">
                            <div class="row">
                                <div class="col-lg-6">
                                   <% list.each do |education| %>
                         <h3><%= education.institute %></h3>
                           <h4><%= education.specilization %><% if education.specilization.present? %>-<% end %><%= education.year_of_pass %><% if education.year_of_pass.present? %>-<% end %><%= education.cgpa_percentage %><% if education.cgpa_percentage.present? %>%<% end %> </h4>
                           
                           <ul class="navigation-btns" style="list-style: none;display:inline;">
                           <li><%= link_to 'Edit', edit_employee_education_path(@employee.id, education.id), :class => "btn btn-success pull-right" %><li>
                           <li><%= link_to 'Delete', edit_employee_education_path(@employee.id, education.id), :class => "btn btn-danger pull-right" %><li> </ul>
                         <hr>
                                   <% end %>
                                </div>
                                <!-- /.col-lg-6 (nested) -->
                                <div class="col-lg-6">
                                    <h1></h1>
                                    <form role="form">
                                    <!--    <fieldset disabled> -->
                        
                                    </form>
                                </div>
                                <!-- /.col-lg-6 (nested) -->
                            </div>
                            <!-- /.row (nested) -->
                        </div>
                        <!-- /.panel-body -->
                    </div>
                    <!-- /.panel -->
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
        </div>

